{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "argo-history.serviceAccountName" . }}
  labels:
    {{- include "argo-history.labels" . | nindent 4 }}
{{- end }}
